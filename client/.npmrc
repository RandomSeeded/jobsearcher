# eslint-plugin-react@7.37.5 only declares a peer dep on eslint <=9.7, but this
# project runs eslint 10. The plugin works fine on 10; this avoids the ERESOLVE
# install failure. Remove once eslint-plugin-react ships eslint 10 peer support.
legacy-peer-deps=true
